# Patch Output

This directory is reserved for patch workflow notes and small metadata.

Do not commit generated binary patches unless the project later decides they are safe and legally distributable. Patch generation should compare a clean legally obtained source image against a locally rebuilt translated image.

